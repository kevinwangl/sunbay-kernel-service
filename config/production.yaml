server:
  host: 0.0.0.0
  port: 3000

database:
  path: data/kernels.db

storage:
  path: static/kernels

# 生产环境从环境变量读取
signing_key: "${SIGNING_KEY}"

cache:
  enabled: true
  max_size_mb: 200

logging:
  level: info
